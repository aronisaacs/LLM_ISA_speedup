"""Named models, tasks, and compressions for run files.

Run files still own nested loops (which configurations to execute). This package
holds the reusable pieces so a new method or task is defined once.
"""
